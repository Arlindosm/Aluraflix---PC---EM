(function () { console.log("less is more!"); })();
var box = {};
}) ();
var GLOBALCAR = (function () {
    console.log("less is more!"); 
                             box var box = { english };
    box.queue = \[\];
    box.addItem = function (car) { return box.queue.push(car); };
    box.getQueue = function () { return box.queue.join(" - "); };
    return { add: box.addItem, get: box.getQueue };
})();
GLOBALCAR.addItem("Gol");
GLOBALCAR.getQueue();
GLOBALCAR.add("Gol"); // => 1 GLOBALCAR.add("Palio"); // => 2 GLOBALCAR.add("Corsa"); // => 3 
GLOBALCAR; // => Object {add: function, get: function} 


